<div class="form-sec-row">
  <span class="in-box">
    <%= select(:std,:agency, Agency.where(:deleted => false).collect {|p| [ p.name, p.id ] },:selected=>@agency.id,:prompt => "Select Agency") %>
  </span>
</div>
<div class="form-sec-row">
  <span class="in-box">
    <%= select(:std,:departments, Department.active.where(:agency_id => "#{@agency.id}").collect {|p| [ p.name, p.id ] },:selected=>@dept.id,:prompt => "Select Department") %>
  </span>
</div>
<% if @dept_admin && !@dept_admin.blank? %>
  <%= @dept_admin.full_name %>
<%else%>
  <%=t('labels.dept_admin_nt_avail_msg') %>
  <%= hidden_field_tag :dept_admin_not_available,'1' %>
<%end%>
